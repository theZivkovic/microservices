### Microservices

An umbrella project for the projects assembling the microservice system:

- items-service: a .NET microservice for managing items
- audit-logs-service: a NodeJS microservice for managing audit-logs
- migrations: a project that builds into a Kubernetes job that handles the database migrations
- charts - helms charts for deploying all resources to Kubernetes cluster
